Follow instructions: https://github.com/DenBot/yasb (might need to install some modules manually)
After cloning the yasb repo, rename src/main.py to main.pyw (this will hide the prompt window)
Create a shortcut and move it to `shell:startup`
Move to ~/.yasb/
